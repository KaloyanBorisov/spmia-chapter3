management:
  endpoints:
    web:
      exposure:
        include: '*'
spring:
  application:
    name: licensingservice
  profiles:
    active:
      default
  cloud:
    config:
      uri: http://localhost:8888
